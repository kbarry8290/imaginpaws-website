{
  "name": "imaginpaws-site",
  "version": "1.0.0",
  "description": "Static site for ImaginPaws AI pet transformations",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./dist/output.css --watch",
    "build": "tailwindcss -i ./src/input.css -o ./dist/output.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
